#include "gtest/gtest.h"

// Tests sendData().

// Test sending zero bits.
TEST(inputa,inputb) {
    EXPECT_EQ(0, 0);
}

// Test sending zero bits.
TEST(inputc,inputd) {
    EXPECT_EQ(1, 1);
}


// Test sending zero bits.
TEST(inputr,inputf) {
    EXPECT_EQ(1, 1);
}
